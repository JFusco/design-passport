# Designer-feedback review remediation

## Objective

Correct the eight defects reproduced while reviewing `aec7cf7..7e4c543`, restore trustworthy certificate freshness and inheritance evidence, and keep publication on hold until the remaining native and organization-user acceptance is complete.

## Executed plan

1. Hash every rule-relevant graph input, including layout-item sizing, detachment acknowledgement, instance inheritance evidence, source/page ownership, and structural indexes.
2. Normalize Figma's native `fontName` override to font family, style, and weight coverage fields.
3. Treat effect dimensions as scale-sensitive inherited evidence.
4. Apply the Layer naming policy to exported-asset naming findings.
5. Resolve catalog vocabulary at component-set or standalone-component identity, excluding child variant syntax.
6. Mark development certificates on both canvas annotations and relaunch labels.
7. Stamp and hash build artifacts, pin development builds, and reject production-stamped or mismatched development copies.
8. Reject dirty production builds and expose a dirty-content suffix on development identities.

## Delivery guard

Commit, pull request, and merge may proceed after repository verification. Organization publication and rollout communication remain held until native Figma inheritance evidence, two non-publisher organization checks, and the separately tracked acceptance follow-up are complete.
